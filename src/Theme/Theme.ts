import { createTheme } from "@mui/material";

export const theme = createTheme({

    palette: {
        primary: {
            main: "#3D52A0"
        },
        secondary: {
            main: "#7091E6"
        }
    },

    typography: {
        allVariants: {
            fontFamily: "Roboto",
            fontSize: "larger"
        }
    }

});